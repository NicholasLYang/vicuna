enum Token {
  Plus,
  Minus,
  Star,
  Slash,
  Integer(i32)
}

fn tokenize(input: string) {
  let i = 0;
  for s in input {
       print(s);
       print(i);
       i = i + 1;
  }
}

tokenize("hello world");
