enum Token {
  Plus,
  Minus,
  Star,
  Slash,
  Integer(i32)
}

fn tokenize(input: string) {
  let out = [];
  let start = 0;
  let i = 0;
  for s in input {
       if s == " " {
         out.push(input.substring(start, i));
       }
       i = i + 1;
  }
}

tokenize("hello world");
