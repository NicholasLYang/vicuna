enum Token {
  Plus,
  Minus,
  Star,
  Slash,
  Integer(i32)
}

fn tokenize(input: string) {
  let out = [];
  for s in input {
       match s {
          '+' => {
              out.push(Token::Plus);
          },
          '-' => {
              out.push(Token::Minus);
          },
          '*' => {
                out.push(Token::Star);
          },
          '/' => {
                out.push(Token::Slash);
          },
          _ => {}
       }
  }

  print(out);
}

tokenize("+-*/");
