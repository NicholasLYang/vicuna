struct Student {
  name: string,
  age: i32
}

fn get_name(student: Student) -> string {
  print(student.name);

  student.name
}

fn new() -> Student {
  let student = Student { name: "John", age: 20 };

  student
}
